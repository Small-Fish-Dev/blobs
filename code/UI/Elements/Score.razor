@namespace Blobs.UI
@inherits Panel

<root>
	@($"{Count:n0}")
</root>

@code {
	private BlobController Controller => Client.Local.IsValid() ? Client.Local.Pawn as BlobController : null;
	private int Count => Controller?.TotalSize ?? 0;

	protected override int BuildHash()
	{
		return HashCode.Combine(Count);
	}
}
